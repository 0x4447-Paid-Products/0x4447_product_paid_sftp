{
	"SNSTopic": {
		"Type": "AWS::SNS::Topic",
		"Properties": {
			"DisplayName": { "Fn::Sub": "sftp_user_single_${UniqueIdentifierParam}_${AWS::Region}" },
			"TopicName": { "Fn::Sub": "sftp_user_single_${UniqueIdentifierParam}_${AWS::Region}" },
			"Tags": [
				{
					"Key": "Description",
					"Value": "Send out messages about EC2 alarms that monitor the basic health of an instance."
				}
			]
		}
	}
}
